## 2.6. Unified and System Memory（统一内存与系统内存）

> Heterogeneous systems have multiple physical memories where data can be stored. The host CPU has attached DRAM, and every GPU in a system has its own attached DRAM. Performance is best when data is resident in the memory of the processor accessing it. CUDA provides APIs to explicitly manage memory placement, but this can be verbose and complicate software design. CUDA provides features and capabilities aimed at easing allocation, placement, and migration of data between different physical memories.

异构系统具有多个可用于存储数据的物理内存。host CPU 连接着自己的 DRAM，系统中的每个 GPU 也连接着各自的 DRAM。当数据驻留在访问它的处理器所连接的内存中时，性能最佳。CUDA 提供了用于显式管理内存放置位置的 API，但这样做可能需要编写大量代码，并使软件设计复杂化。为此，CUDA 提供了一些功能，旨在简化数据在不同物理内存之间的分配、放置和迁移。

> The purpose of this chapter is to introduce and explain these features and what they mean to application developers for both functionality and performance. Unified memory has several different manifestations which depend upon the OS, driver version, and GPU used. This chapter will show how to determine which unified memory paradigm applies and how the features of unified memory behave in each. The later chapter on unified memory explains unified memory in more detail.

本章旨在介绍并解释这些功能，以及它们对应用开发者在功能和性能方面的意义。Unified memory 存在几种不同的实现形式，具体取决于操作系统、驱动版本和所用 GPU。本章将说明如何确定系统采用哪种 unified memory 模式，以及 unified memory 的各项功能在不同模式下的行为。后续关于 unified memory 的章节会对此进行更详细的解释。

> The following concepts will be defined and explained in this chapter:

本章将定义并解释以下概念：

- > Unified Virtual Address Space - CPU memory and each GPU’s memory have a distinct range within a single virtual address space

  Unified Virtual Address Space（统一虚拟地址空间）：CPU 内存和每个 GPU 的内存在同一个虚拟地址空间内分别占据不同的地址范围。

- > Unified Memory - A CUDA feature that enables managed memory which can be automatically migrated between CPU and GPUs

  Unified Memory（统一内存）：一种 CUDA 功能，支持可在 CPU 与 GPU 之间自动迁移的 managed memory（托管内存）。

- > Limited Unified Memory - A unified memory paradigm with some limitations

  Limited Unified Memory（受限统一内存）：一种具有部分限制的 unified memory 模式。

- > Full Unified Memory - Full support for unified memory features

  Full Unified Memory（完整统一内存）：完整支持 unified memory 功能。

- > Full Unified Memory with Hardware Coherency - Full support for unified memory using hardware capabilities

  Full Unified Memory with Hardware Coherency（具有硬件一致性的完整统一内存）：借助硬件能力完整支持 unified memory。

- > Unified memory hints - APIs to guide unified memory behavior for specific allocations

  Unified memory hints（统一内存提示）：用于指导特定内存分配的 unified memory 行为的 API。

- > Page-locked Host Memory - Non-pageable system memory, which is necessary for some CUDA operations

  Page-locked Host Memory（页锁定主机内存）：不可分页的系统内存，某些 CUDA 操作需要使用这类内存。

- > Mapped memory - A mechanism (different from unified memory) for accessing host memory directly from a kernel

  Mapped memory（映射内存）：一种让 kernel 直接访问 host 内存的机制，与 unified memory 不同。

> Additionally, the following terms used when discussing unified and system memory are introduced here:

此外，本章还介绍了讨论 unified memory 和 system memory 时使用的以下术语：

- > Heterogeneous Managed Memory (HMM) - A feature of the Linux kernel that enables software coherency for full unified memory

  Heterogeneous Managed Memory（HMM，原文如此）：Linux kernel 的一项功能，通过软件一致性支持完整的 unified memory。

**解读（术语勘误）：** 此处原文将 HMM 展开为 Heterogeneous Managed Memory；本章后文采用的正确名称是 Heterogeneous Memory Management（异构内存管理）。英文原句保留供对照。

- > Address Translation Services (ATS) - A hardware feature, available when GPUs are connected to the CPU by the NVLink Chip-to-Chip (C2C) interconnect, which provides hardware coherency for full unified memory

  Address Translation Services（ATS，地址转换服务）：GPU 通过 NVLink Chip-to-Chip（C2C）互连连接到 CPU 时可用的一项硬件功能，为完整的 unified memory 提供硬件一致性。

### 2.6.1. Unified Virtual Address Space（统一虚拟地址空间）

> A single virtual address space is used for all host memory and all global memory on all GPUs in the system within a single OS process. All memory allocations on the host and on all devices lie in this virtual address space. This is true whether allocations are made with CUDA APIs (e.g. `cudaMalloc`, `cudaMallocHost`) or with system allocation APIs (e.g. new, malloc, mmap). The CPU and each GPU has a unique range within the unified virtual address space.

在单个操作系统进程内，系统中的所有 host 内存以及所有 GPU 上的 global memory 都使用同一个虚拟地址空间。host 和所有 device 上的内存分配都位于这个虚拟地址空间内。无论使用 CUDA API（例如 `cudaMalloc`、`cudaMallocHost`），还是系统内存分配 API（例如 `new`、`malloc`、`mmap`），情况都是如此。CPU 和每个 GPU 在统一虚拟地址空间中分别具有独有的地址范围。

> This means:

这意味着：

- > The location of any memory (that is, CPU or which GPU’s memory it lies in) can be determined from the value of a pointer using `cudaPointerGetAttributes()`

  可以根据指针值，使用 `cudaPointerGetAttributes()` 确定任意内存的位置，即它位于 CPU 内存还是某个 GPU 的内存中。

- > The `cudaMemcpyKind` parameter of `cudaMemcpy*()` can be set to `cudaMemcpyDefault` to automatically determine the copy type from the pointers

  可以将 `cudaMemcpy*()` 的 `cudaMemcpyKind` 参数设为 `cudaMemcpyDefault`，从而根据指针自动确定复制类型。

**解读（地址与位置）：** UVA 统一的是虚拟地址空间；同一个指针可用于识别分配类型，并不意味着所有处理器都能直接解引用它。原文对 `cudaPointerGetAttributes()` 的“位置”描述较宽泛：该 API 返回分配属性，不能用来查询 managed memory 每一页当前驻留在哪里；其 `device` 字段也不能一概视为当前物理位置。参见 [CUDA Runtime：Unified Addressing](https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__UNIFIED.html)。

### 2.6.2. Unified Memory（统一内存）

> Unified memory is a CUDA memory feature which allows memory allocations called managed memory to be accessed from code running on either the CPU or the GPU. Unified memory was shown in the intro to CUDA in C++. Unified memory is available on all systems supported by CUDA.

Unified memory 是 CUDA 的一项内存功能，使称为 managed memory 的内存分配能够被运行在 CPU 或 GPU 上的代码访问。前面的 CUDA C++ 入门章节已经展示过 unified memory。所有受 CUDA 支持的系统都提供 unified memory。

> On some systems, managed memory must be explicitly allocated. Managed memory can be explicitly allocated in CUDA in a few different ways:

在某些系统上，必须显式分配 managed memory。在 CUDA 中，可以通过以下几种方式显式分配 managed memory：

- > The CUDA API `cudaMallocManaged`

  使用 CUDA API `cudaMallocManaged`。

- > The CUDA API `cudaMallocFromPoolAsync` with a pool created with allocType set to `cudaMemAllocationTypeManaged`

  使用 CUDA API `cudaMallocFromPoolAsync`，并传入创建时将 `allocType` 设为 `cudaMemAllocationTypeManaged` 的 memory pool。

- > Global variables with the `__managed__` specifier (see Memory Space Specifiers)

  使用带有 `__managed__` 限定符的 global variable，参见 Memory Space Specifiers（内存空间限定符）。

> On systems with HMM or ATS, all system memory is implicitly managed memory, regardless of how it is allocated. No special allocation is needed.

在支持 HMM 或 ATS 的系统上，无论如何分配，所有 system memory 都隐式属于 managed memory，无需采用特殊的分配方式。

#### 2.6.2.1. Unified Memory Paradigms（统一内存的支持模式）

> The features and behavior of unified memory vary between operating systems, kernel versions on Linux, GPU hardware, and the GPU-CPU interconnect. The form of unified memory available can be determined by using `cudaDeviceGetAttribute` to query a few attributes:

Unified memory 的功能和行为因操作系统、Linux kernel 版本、GPU 硬件以及 GPU 与 CPU 之间的互连而异。可以使用 `cudaDeviceGetAttribute` 查询以下属性，确定系统提供哪种形式的 unified memory：

- > `cudaDevAttrConcurrentManagedAccess` - 1 for full unified memory support, 0 for limited support

  `cudaDevAttrConcurrentManagedAccess`：值为 1 表示完整支持 unified memory，值为 0 表示支持受限。

- > `cudaDevAttrPageableMemoryAccess` - 1 means all system memory is fully-supported unified memory, 0 means only memory explicitly allocated as managed memory is fully-supported unified memory

  `cudaDevAttrPageableMemoryAccess`：值为 1 表示所有 system memory 都是具有完整支持的 unified memory；值为 0 表示只有显式分配为 managed memory 的内存才具有完整的 unified memory 支持。

- > `cudaDevAttrPageableMemoryAccessUsesHostPageTables` - Indicates the mechanism of CPU/GPU coherence: 1 is hardware, 0 is software.

  `cudaDevAttrPageableMemoryAccessUsesHostPageTables`：表示 CPU 与 GPU 之间的一致性机制；值为 1 表示硬件一致性，值为 0 表示软件一致性。

> Figure 21 illustrates how to determine the unified memory paradigm visually and is followed by a code sample implementing the same logic.

图 21 直观展示了如何确定 unified memory 的支持模式，后面还提供了实现同一判断逻辑的代码示例。

> There are four paradigms of unified memory operation:

Unified memory 有以下四种工作模式：

- > Full support for explicit managed memory allocations

  对显式分配的 managed memory 提供完整支持。

- > Full support for all allocations with software coherence

  通过软件一致性，对所有内存分配提供完整支持。

- > Full support for all allocations with hardware coherence

  通过硬件一致性，对所有内存分配提供完整支持。

- > Limited unified memory support

  对 unified memory 提供受限支持。

> When full support is available, it can either require explicit allocations, or all system memory may implicitly be unified memory. When all memory is implicitly unified, the coherence mechanism can either be software or hardware. Windows and some Tegra devices have limited support for unified memory.

在具有完整支持的情况下，系统可能要求显式分配 managed memory，也可能使所有 system memory 隐式成为 unified memory。当所有内存都隐式统一管理时，一致性机制可以由软件或硬件提供。Windows 和部分 Tegra device 对 unified memory 的支持受限。

**解读：** 三个属性应按顺序判断：先确认是否具有完整支持，再判断支持范围是否覆盖系统分配，最后判断一致性机制。不能仅凭 `cudaDevAttrPageableMemoryAccessUsesHostPageTables == 0` 就断定系统属于“完整支持的软件一致性”模式。这里的 full 描述功能支持范围，不能推导出任意访问都有相同的性能。

> Figure 21 All current GPUs use a unified virtual address space and have unified memory available. When `cudaDevAttrConcurrentManagedAccess` is 1, full unified memory support is available, otherwise only limited support is available. When full support is available, if `cudaDevAttrPageableMemoryAccess` is also 1, then all system memory is unified memory. Otherwise, only memory allocated with CUDA APIs (such as `cudaMallocManaged`) is unified memory. When all system memory is unified, `cudaDevAttrPageableMemoryAccessUsesHostPageTables` indicates whether coherence is provided by hardware (when value is 1) or software (when value is 0).

图 21：当前所有 GPU 都使用统一虚拟地址空间，并提供 unified memory。当 `cudaDevAttrConcurrentManagedAccess` 为 1 时，系统提供完整的 unified memory 支持，否则仅提供受限支持。在提供完整支持的前提下，如果 `cudaDevAttrPageableMemoryAccess` 也为 1，那么所有 system memory 都是 unified memory；否则，只有通过 CUDA API（例如 `cudaMallocManaged`）分配的内存才是 unified memory。当所有 system memory 都是 unified memory 时，`cudaDevAttrPageableMemoryAccessUsesHostPageTables` 表示一致性由硬件提供（值为 1）还是由软件提供（值为 0）。

**整理说明：** 本次提供的文本包含图 21 的图注，未包含图片；图注完整保留，下表呈现同一判断关系。表内文档标题也按原文保留，粘贴文本未提供其链接。

> Table 3 shows the same information as Figure 21 as a table with links to the relevant sections of this chapter and more complete documentation in a later section of this guide.

表 3 以表格形式呈现了与图 21 相同的信息，并链接到本章相关小节以及本指南后续章节中更完整的文档。

> Table 3 Overview of Unified Memory Paradigms

表 3：Unified Memory 支持模式概览。

> | Unified Memory Paradigm | Device Attributes | Full Documentation |
> | --- | --- | --- |
> | Limited unified memory support | `cudaDevAttrConcurrentManagedAccess` is 0 | Unified Memory on Windows, WSL, and Tegra<br>CUDA for Tegra Memory Management<br>Unified memory on Tegra |
> | Full support for explicit managed memory allocations | `cudaDevAttrPageableMemoryAccess` is 0<br>and `cudaDevAttrConcurrentManagedAccess` is 1 | Unified Memory on Devices with only CUDA Managed Memory Support |
> | Full support for all allocations with software coherence | `cudaDevAttrPageableMemoryAccessUsesHostPageTables` is 0<br>and `cudaDevAttrPageableMemoryAccess` is 1<br>and `cudaDevAttrConcurrentManagedAccess` is 1 | Unified Memory on Devices with Full CUDA Unified Memory Support |
> | Full support for all allocations with hardware coherence | `cudaDevAttrPageableMemoryAccessUsesHostPageTables` is 1<br>and `cudaDevAttrPageableMemoryAccess` is 1<br>and `cudaDevAttrConcurrentManagedAccess` is 1 | Unified Memory on Devices with Full CUDA Unified Memory Support |

| Unified Memory 支持模式 | Device Attribute 条件 | 完整文档 |
| --- | --- | --- |
| 受限的 unified memory 支持 | `cudaDevAttrConcurrentManagedAccess` 为 0 | Windows、WSL 和 Tegra 上的统一内存<br>CUDA for Tegra 内存管理<br>Tegra 上的统一内存 |
| 完整支持显式的 managed memory allocation | `cudaDevAttrPageableMemoryAccess` 为 0<br>且 `cudaDevAttrConcurrentManagedAccess` 为 1 | 仅支持 CUDA Managed Memory 的设备上的统一内存 |
| 通过软件一致性完整支持所有内存分配 | `cudaDevAttrPageableMemoryAccessUsesHostPageTables` 为 0<br>且 `cudaDevAttrPageableMemoryAccess` 为 1<br>且 `cudaDevAttrConcurrentManagedAccess` 为 1 | 具有完整 CUDA Unified Memory 支持的设备上的统一内存 |
| 通过硬件一致性完整支持所有内存分配 | `cudaDevAttrPageableMemoryAccessUsesHostPageTables` 为 1<br>且 `cudaDevAttrPageableMemoryAccess` 为 1<br>且 `cudaDevAttrConcurrentManagedAccess` 为 1 | 具有完整 CUDA Unified Memory 支持的设备上的统一内存 |

##### 2.6.2.1.1. Unified Memory Paradigm: Code Example（统一内存支持模式：代码示例）

> The following code example demonstrates querying the device attributes and determining the unified memory paradigm, following the logic of Figure 21, for each GPU in a system.

下面的代码示例遵循图 21 的逻辑，展示了如何针对系统中的每个 GPU 查询 device attribute，并确定其 unified memory 支持模式。

```cpp
void queryDevices()
{
    int numDevices = 0;
    cudaGetDeviceCount(&numDevices);
    for(int i=0; i<numDevices; i++)
    {
        cudaSetDevice(i);
        cudaInitDevice(0, 0, 0);
        int deviceId = i;

        int concurrentManagedAccess = -1;
        cudaDeviceGetAttribute (&concurrentManagedAccess, cudaDevAttrConcurrentManagedAccess, deviceId);
        int pageableMemoryAccess = -1;
        cudaDeviceGetAttribute (&pageableMemoryAccess, cudaDevAttrPageableMemoryAccess, deviceId);
        int pageableMemoryAccessUsesHostPageTables = -1;
        cudaDeviceGetAttribute (&pageableMemoryAccessUsesHostPageTables, cudaDevAttrPageableMemoryAccessUsesHostPageTables, deviceId);

        printf("Device %d has ", deviceId);
        if(concurrentManagedAccess){
            if(pageableMemoryAccess){
                printf("full unified memory support");
                if( pageableMemoryAccessUsesHostPageTables)
                    { printf(" with hardware coherency\n");  }
                else
                    { printf(" with software coherency\n"); }
            }
            else
                { printf("full unified memory support for CUDA-made managed allocations\n"); }
        }
        else
        {   printf("limited unified memory support: Windows, WSL, or Tegra\n");  }
    }
}
```

**解读（示例问题）：** 循环中的 `cudaInitDevice(0, 0, 0)` 始终初始化 device 0，并不是初始化当前的第 `i` 个 device；若意图初始化遍历到的设备，应检查这里的参数。该调用不会切换当前 device，后面的属性查询也显式传入了 `deviceId = i`，因此不能据此说所有查询都变成了查询 device 0。更直接的可靠性问题是原例未检查 API 返回值：查询失败后属性可能仍为 `-1`，而 C++ 的 `if(-1)` 为真，会导致错误分类。应先确认查询成功，再解释属性值。参见 [CUDA Runtime：Device Management](https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__DEVICE.html)。

#### 2.6.2.2. Full Unified Memory Feature Support（完整的统一内存功能支持）

> Most Linux systems have full unified memory support. If device attribute `cudaDevAttrPageableMemoryAccess` is 1, then all system memory, whether allocated by CUDA APIs or system APIs, operates as unified memory with full feature support. This includes file-backed memory allocations created with mmap.

大多数 Linux 系统提供完整的 unified memory 支持。如果 device attribute `cudaDevAttrPageableMemoryAccess` 为 1，那么所有 system memory，无论由 CUDA API 还是系统 API 分配，都作为具有完整功能支持的 unified memory 工作。这也包括使用 `mmap` 创建的 file-backed memory allocation（由文件作为后备存储的内存分配）。

> If `cudaDevAttrPageableMemoryAccess` is 0, then only memory allocated as managed memory by CUDA behaves as unified memory. Memory allocated with system APIs is not managed and is not necessarily accessible from GPU kernels.

如果 `cudaDevAttrPageableMemoryAccess` 为 0，则只有 CUDA 分配为 managed memory 的内存才按照 unified memory 的方式工作。通过系统 API 分配的内存不受托管，也不一定能被 GPU kernel 访问。

> In general, for unified allocations with full support:

通常，对于具有完整支持的 unified memory allocation：

- > Managed memory is usually allocated in the memory space of the processor where it is first touched

  Managed memory 通常在首次访问它的处理器所在的内存空间中分配。

- > Managed memory is usually migrated when it is used by a processor other than the processor where it currently resides

  当 managed memory 被其当前驻留位置以外的处理器使用时，通常会发生迁移。

- > Managed memory is migrated or accessed at the granularity of memory pages (software coherence) or cache lines (hardware coherence)

  Managed memory 以 memory page（软件一致性）或 cache line（硬件一致性）为粒度进行迁移或访问。

- > Oversubscription is allowed: an application may allocate more managed memory than is physically available on the GPU

  允许 oversubscription（超额分配）：应用可以分配超过 GPU 实际物理内存容量的 managed memory。

> Allocation and migration behavior can deviate from the above. This can by influenced the programmer using hints and prefetches. Full coverage of full unified memory support can be found in Unified Memory on Devices with Full CUDA Unified Memory Support.

实际的分配和迁移行为可能与上述情况不同。程序员可以通过 hint 和 prefetch 影响这些行为。关于完整 unified memory 支持的全面说明，参见 Unified Memory on Devices with Full CUDA Unified Memory Support（具有完整 CUDA Unified Memory 支持的设备上的统一内存）。

**解读：** First touch 说明首次访问可能影响物理放置；后续访问模式又可能触发迁移。因此，代码中能直接使用同一个指针，并不代表数据传输成本消失。反复在 CPU 与 GPU 之间交替访问同一批数据，可能增加迁移开销；oversubscription 也只说明允许超出 GPU 容量，不能保证与全部数据驻留 GPU 时具有相同的性能。原文使用 usually，明确保留了实现策略和 hint 的影响。

##### 2.6.2.2.1. Full Unified Memory with Hardware Coherency（具有硬件一致性的完整统一内存）

> On hardware such as Grace Hopper and Grace Blackwell, where an NVIDIA CPU is used and the interconnect between the CPU and GPU is NVLink Chip-to-Chip (C2C), address translation services (ATS) are available. `cudaDevAttrPageableMemoryAccessUsesHostPageTables` is 1 when ATS is available.

在 Grace Hopper 和 Grace Blackwell 等硬件上，系统采用 NVIDIA CPU，CPU 与 GPU 通过 NVLink Chip-to-Chip（C2C）互连连接，并提供 Address Translation Services（ATS）。当 ATS 可用时，`cudaDevAttrPageableMemoryAccessUsesHostPageTables` 为 1。

> With ATS, in addition to full unified memory support for all host allocations:

使用 ATS 时，除了对所有 host allocation 提供完整的 unified memory 支持，还具有以下能力：

- > Managed allocations resident on the GPU (e.g. `cudaMallocManaged`) can be accessed from the CPU without migration (`cudaDevAttrDirectManagedMemAccessFromHost` will be 1)

  CPU 可以在不迁移数据的情况下访问驻留在 GPU 上的 managed allocation，例如通过 `cudaMallocManaged` 分配的内存；此时 `cudaDevAttrDirectManagedMemAccessFromHost` 为 1。

- > The link between CPU and GPU supports native atomics (`cudaDevAttrHostNativeAtomicSupported` will be 1)

  CPU 与 GPU 之间的互连支持 native atomic operation，此时 `cudaDevAttrHostNativeAtomicSupported` 为 1。

- > Hardware support for coherence can improve performance compared to software coherence

  与软件一致性相比，通过硬件支持一致性可以改善性能。

> ATS provides all capabilities of HMM. When ATS is available, HMM is automatically disabled. Further discussion of hardware vs. software coherency is found in CPU and GPU Page Tables: Hardware Coherency vs. Software Coherency.

ATS 提供 HMM 的全部能力。当 ATS 可用时，HMM 会被自动禁用。关于硬件一致性与软件一致性的进一步讨论，参见 CPU and GPU Page Tables: Hardware Coherency vs. Software Coherency（CPU 与 GPU 页表：硬件一致性与软件一致性）。

> Note: Hardware coherency does not enable access to GPU-only allocations such as those made with `cudaMalloc` from the host.

注意：硬件一致性并不意味着 host 可以访问仅供 GPU 使用的内存分配，例如通过 `cudaMalloc` 分配的内存。

**解读：** 硬件一致性、访问权限和数据放置是不同问题。ATS 支持直接访问，并不意味着 `cudaMalloc` 分配变成 host 可解引用的内存，也不意味着所有 managed allocation 都自动采用“不迁移”的策略；具体行为仍受配置影响。参见 [Unified Memory 官方说明](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/unified-memory.html)。

##### 2.6.2.2.2. HMM - Full Unified Memory with Software Coherency（HMM：具有软件一致性的完整统一内存）

> Heterogeneous Memory Management (HMM) is a feature available on Linux operating systems (with appropriate kernel versions) which enables software-coherent full unified memory support. Heterogeneous memory management brings some of the capabilities and convenience provided by ATS to PCIe-connected GPUs.

Heterogeneous Memory Management（HMM，异构内存管理）是具有适当 kernel 版本的 Linux 操作系统提供的一项功能，支持基于软件一致性的完整 unified memory。HMM 将 ATS 提供的部分能力和便利带到了通过 PCIe 连接的 GPU 上。

> On Linux with at least Linux Kernel 6.1.24, 6.2.11, or 6.3 or later, heterogeneous memory management (HMM) may be available. The following command can be used to find if the addressing mode is HMM.

在 Linux kernel 版本至少为 6.1.24、6.2.11，或 6.3 及更高版本的 Linux 系统上，HMM 可能可用。可以使用以下命令检查 addressing mode 是否为 HMM。

```text
$ nvidia-smi -q | grep Addressing
Addressing Mode : HMM
```

> When HMM is available, full unified memory is supported and all system allocations are implicitly unified memory. If a system also has ATS, HMM is disabled and ATS is used, since ATS provides all the capabilities of HMM and more.

当 HMM 可用时，系统支持完整的 unified memory，所有通过系统接口进行的内存分配都隐式属于 unified memory。如果系统同时具有 ATS，则会禁用 HMM 并使用 ATS，因为 ATS 提供 HMM 的全部能力以及额外能力。

**解读（环境条件）：** 达到列出的 Linux kernel 版本只是条件之一，还需满足 GPU、驱动及内核模块要求。应结合设备属性判断，不能只看版本号或命令示例中的 `HMM` 输出。上面的输出来自原文，未在本机执行。参见 [HMM 支持条件](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/unified-memory.html)。

#### 2.6.2.3. Limited Unified Memory Support（受限的统一内存支持）

> On Windows, including Windows Subsystem for Linux (WSL), and on some Tegra systems, a limited subset of unified memory functionality is available. On these systems, managed memory is available, but migration between CPU and GPUs behaves differently.

在 Windows（包括 Windows Subsystem for Linux，WSL）以及部分 Tegra 系统上，只能使用 unified memory 功能的一个受限子集。这些系统仍然提供 managed memory，但 CPU 与 GPU 之间的迁移行为有所不同。

- > Managed memory is first allocated in the CPU’s physical memory

  Managed memory 最初分配在 CPU 的物理内存中。

- > Managed memory is migrated in larger granularity than virtual memory pages

  Managed memory 的迁移粒度大于虚拟内存页。

- > Managed memory is migrated to the GPU when the GPU begins executing

  GPU 开始执行时，managed memory 会迁移到 GPU。

- > The CPU must not access managed memory while the GPU is active

  GPU 处于活动状态时，CPU 不得访问 managed memory。

- > Managed memory is migrated back to the CPU when the GPU is synchronized

  当 GPU 完成同步时，managed memory 会迁回 CPU。

- > Oversubscription of GPU memory is not allowed

  不允许超额分配 GPU 内存。

- > Only memory explicitly allocated by CUDA as managed memory is unified

  只有由 CUDA 显式分配为 managed memory 的内存才会受到统一管理。

> Full coverage of this paradigm can be found in Unified Memory on Windows, WSL, and Tegra.

关于这一模式的全面说明，参见 Unified Memory on Windows, WSL, and Tegra（Windows、WSL 和 Tegra 上的统一内存）。

**解读（原文限定）：** “最初分配在 CPU”以及“同步时迁回 CPU”不应理解为所有受限平台的统一物理放置契约；实际行为还受平台、多 GPU 配置和访问触发的缺页处理影响。同步的核心作用是确保相关 GPU 工作完成，使后续 CPU 访问合法，不能单凭同步推断全部数据已搬回 CPU。参见 [受限 Unified Memory 的完整说明](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/unified-memory.html)。

#### 2.6.2.4. Memory Advise and Prefetch（内存提示与预取）

> The programmer can provide hints to the NVIDIA Driver managing unified memory to help it maximize application performance. The CUDA API `cudaMemAdvise` allows the programmer to specify properties of allocations that affect where they are placed and whether or not the memory is migrated when accessed from another device.

程序员可以向管理 unified memory 的 NVIDIA Driver 提供 hint，帮助它尽可能提高应用性能。CUDA API `cudaMemAdvise` 允许程序员指定内存分配的属性，从而影响内存放置在哪里，以及当其他 device 访问这块内存时是否发生迁移。

> `cudaMemPrefetchAsync` allows the programmer to suggest an asynchronous migration of a specific allocation to a different location be started. A common use is starting the transfer of data a kernel will use before the kernel is launched. This enables the copy of data to occur while other GPU kernels are executing.

`cudaMemPrefetchAsync` 允许程序员建议开始将某个特定内存分配异步迁移到其他位置。常见用途是在 kernel 启动之前，就开始传输该 kernel 将要使用的数据。这样，数据复制便有机会与其他 GPU kernel 的执行重叠。

> The section on Performance Hints covers the different hints that can be passed to `cudaMemAdvise` and shows examples of using `cudaMemPrefetchAsync`.

Performance Hints（性能提示）一节介绍了可以传递给 `cudaMemAdvise` 的不同 hint，并展示了使用 `cudaMemPrefetchAsync` 的示例。

**解读：** `cudaMemAdvise` 提供放置和访问策略提示；`cudaMemPrefetchAsync` 提前安排数据移动。要让预取先于消费数据的 kernel 完成，可以在同一 stream 中依次提交；若分属不同 stream，则需通过 event 等机制表达依赖。异步预取的提交本身不表示迁移已经完成，能否与其他工作重叠仍取决于依赖和执行资源。

### 2.6.3. Page-Locked Host Memory（页锁定主机内存）

> In introductory code examples, `cudaMallocHost` was used to allocate memory on the CPU. This allocates page-locked memory (also known as pinned memory) on the host. Host allocations made through traditional allocation mechanisms like malloc, new, or mmap are not page-locked, which means they may be swapped to disk or physically relocated by the operating system.

在前面的入门代码示例中，使用 `cudaMallocHost` 在 CPU 上分配内存。该函数在 host 上分配 page-locked memory（页锁定内存，也称 pinned memory）。通过 `malloc`、`new` 或 `mmap` 等传统机制进行的 host allocation 不会锁页，这意味着操作系统可能将这些内存换出到磁盘，或改变其物理位置。

> Page-locked host memory is required for asynchronous copies between the CPU and GPU. Page-locked host memory also improves performance of synchronous copies. Page-locked memory can be mapped to the GPU for direct access from GPU kernels.

CPU 与 GPU 之间的异步复制需要 page-locked host memory。页锁定主机内存也能提高同步复制的性能。Page-locked memory 可以映射到 GPU，让 GPU kernel 直接访问。

> The CUDA runtime provides APIs to allocate page-locked host memory or to page-lock existing allocations:

CUDA Runtime 提供了用于分配 page-locked host memory 或锁定现有内存分配的 API：

- > `cudaMallocHost` allocates page-locked host memory

  `cudaMallocHost` 分配 page-locked host memory。

- > `cudaHostAlloc` defaults to the same behavior as `cudaMallocHost`, but also takes flags to specify other memory parameters

  `cudaHostAlloc` 默认具有与 `cudaMallocHost` 相同的行为，但还接受用于指定其他内存参数的 flag。

- > `cudaFreeHost` frees memory allocated with `cudaMallocHost` or `cudaHostAlloc`

  `cudaFreeHost` 释放通过 `cudaMallocHost` 或 `cudaHostAlloc` 分配的内存。

- > `cudaHostRegister` page-locks a range of existing memory allocated outside the CUDA API, such as with malloc or mmap

  `cudaHostRegister` 对 CUDA API 之外分配的一段现有内存进行锁页，例如通过 `malloc` 或 `mmap` 分配的内存。

> `cudaHostRegister` enables host memory allocated by 3rd party libraries or other code outside of a developer’s control to be page-locked so that it can be used in asynchronous copies or mapped.

`cudaHostRegister` 使第三方库或开发者无法控制的其他代码所分配的 host memory 也可以被锁页，从而用于异步复制或内存映射。

**解读：** `cudaHostRegister` 注册的是已有分配，不会把原指针替换成另一块 GPU allocation。它改变该内存与 CUDA 的使用关系，原始分配的所有权仍需由原分配方管理。解除注册使用 `cudaHostUnregister`；内存本身则使用与原分配方式匹配的释放接口。参见 [CUDA Runtime：Memory Management](https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__MEMORY.html)。

> Note: Page-locked host memory can be used for asynchronous copies and mapped-memory by all GPUs in the system.

注意：系统中的所有 GPU 都可以使用 page-locked host memory 进行异步复制和 mapped-memory 访问。

> Page-locked host memory is not cached on non I/O coherent Tegra devices. Also, `cudaHostRegister()` is not supported on non I/O coherent Tegra devices.

在不具备 I/O coherence 的 Tegra device 上，page-locked host memory 不会被缓存。此外，这些 device 不支持 `cudaHostRegister()`。

#### 2.6.3.1. Mapped Memory（映射内存）

> On systems with HMM or ATS, all host memory is directly accessible from the GPU using the host pointers. When ATS or HMM are not available, host allocations can be made accessible to the GPU by mapping the memory into the GPU’s memory space. Mapped memory is always page-locked.

在支持 HMM 或 ATS 的系统上，GPU 可以使用 host pointer 直接访问所有 host memory。当 ATS 或 HMM 不可用时，可以通过将 host allocation 映射到 GPU 的内存空间，使 GPU 能够访问它们。Mapped memory 始终是 page-locked memory。

> The code examples which follow will illustrate the following array copy kernel operating directly on mapped host memory.

下面的代码示例将展示如何让以下数组复制 kernel 直接操作 mapped host memory。

```cpp
__global__ void copyKernel(float* a, float* b)
{
        int idx = threadIdx.x + blockDim.x * blockIdx.x;
        a[idx] = b[idx];
}
```

> While mapped memory may be useful in some cases where certain data which is not copied to the GPU needs to be accessed from a kernel, accessing mapped memory in a kernel requires transactions across the CPU-GPU interconnect, PCIe, or NVLink C2C. These operations have higher latency and lower bandwidth compared to accessing device memory. Mapped memory should not be considered a performant alternative to unified memory or explicit memory management for the majority of a kernel’s memory needs.

在某些情况下，kernel 需要访问尚未复制到 GPU 的特定数据，此时 mapped memory 可能有用。但是，kernel 访问 mapped memory 需要通过 CPU 与 GPU 之间的互连发起事务，例如 PCIe 或 NVLink C2C。与访问 device memory 相比，这些操作的延迟更高、带宽更低。因此，对于 kernel 的大部分内存访问需求，不应将 mapped memory 视为 unified memory 或显式内存管理的高性能替代方案。

##### 2.6.3.1.1. cudaMallocHost and cudaHostAlloc（cudaMallocHost 与 cudaHostAlloc）

> Host memory allocated with `cudaMallocHost` or `cudaHostAlloc` is automatically mapped. The pointers returned by these APIs can be directly used in kernel code to access the memory on the host. The host memory is accessed over the CPU-GPU interconnect.

通过 `cudaMallocHost` 或 `cudaHostAlloc` 分配的 host memory 会自动映射。可以在 kernel 代码中直接使用这些 API 返回的指针，访问 host 上的内存。对 host memory 的访问经过 CPU 与 GPU 之间的互连。

**解读（指针例外）：** 原文适用于这里演示的常见分配方式；带 `cudaHostAllocWriteCombined` flag 的分配可能具有不同的 host 与 device 指针，需要通过 `cudaHostGetDevicePointer()` 获取 device 可用指针，不能把“直接使用返回指针”推广到所有 flag 组合。参见 [Unified Addressing 的例外](https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__UNIFIED.html)。

```cpp
void usingMallocHost() {
  float* a = nullptr;
  float* b = nullptr;

  CUDA_CHECK(cudaMallocHost(&a, vLen*sizeof(float)));
  CUDA_CHECK(cudaMallocHost(&b, vLen*sizeof(float)));

  initVector(b, vLen);
  memset(a, 0, vLen*sizeof(float));

  int threads = 256;
  int blocks = vLen/threads;
  copyKernel<<<blocks, threads>>>(a, b);
  CUDA_CHECK(cudaGetLastError());
  CUDA_CHECK(cudaDeviceSynchronize());

  printf("Using cudaMallocHost: ");
  checkAnswer(a,b);
}
```

**解读（示例边界）：** 原例保留了未定义的 `vLen`、`CUDA_CHECK`、`initVector` 和 `checkAnswer`，属于演示片段。`blocks = vLen / threads` 使用向下取整，配合没有边界检查的 `copyKernel`，要求正数 `vLen` 能被 256 整除；否则尾部元素不会被处理，小于 256 时还会得到零个 block。若改成向上取整，也必须同步为 kernel 增加长度参数和 `idx < vLen` 检查。示例还省略了收尾的 `cudaFreeHost(a)` 与 `cudaFreeHost(b)`，应在 GPU 使用完成后释放。这次仅整理与静态阅读代码，未运行 CUDA 示例。

##### 2.6.3.1.2. cudaHostRegister（cudaHostRegister）

> When ATS and HMM are not available, allocations made by system allocators can still be mapped for access directly from GPU kernels using `cudaHostRegister`. Unlike memory created with CUDA APIs, however, the memory cannot be accessed from the kernel using the host pointer. A pointer in the device’s memory region must be obtained using `cudaHostGetDevicePointer()`, and that pointer must be used for accesses in kernel code.

当 ATS 和 HMM 不可用时，仍然可以使用 `cudaHostRegister` 映射系统分配器分配的内存，使 GPU kernel 直接访问这些内存。不过，与通过 CUDA API 创建的内存不同，kernel 不能使用 host pointer 访问它们。必须使用 `cudaHostGetDevicePointer()` 获取 device 内存地址范围中的指针，并在 kernel 代码中使用该指针进行访问。

**解读（原文限定）：** “不能使用 host pointer”并非所有设备上的绝对规则。若 `cudaDevAttrCanUseHostPointerForRegisteredMem` 非零，设备也可以使用已注册内存的 host pointer。获取并使用 `cudaHostGetDevicePointer()` 返回的 device pointer 是本例采用的方式，两种指针的数值可能相同，也可能不同。参见 [已注册内存的指针规则](https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__MEMORY.html)。

```cpp
void usingRegister() {
  float* a = nullptr;
  float* b = nullptr;
  float* devA = nullptr;
  float* devB = nullptr;

  a = (float*)malloc(vLen*sizeof(float));
  b = (float*)malloc(vLen*sizeof(float));
  CUDA_CHECK(cudaHostRegister(a, vLen*sizeof(float), 0 ));
  CUDA_CHECK(cudaHostRegister(b, vLen*sizeof(float), 0  ));

  CUDA_CHECK(cudaHostGetDevicePointer((void**)&devA, (void*)a, 0));
  CUDA_CHECK(cudaHostGetDevicePointer((void**)&devB, (void*)b, 0));

  initVector(b, vLen);
  memset(a, 0, vLen*sizeof(float));

  int threads = 256;
  int blocks = vLen/threads;
  copyKernel<<<blocks, threads>>>(devA, devB);
  CUDA_CHECK(cudaGetLastError());
  CUDA_CHECK(cudaDeviceSynchronize());

  printf("Using cudaHostRegister: ");
  checkAnswer(a, b);
}
```

**解读（资源生命周期）：** 这里的 `devA`、`devB` 是同一批 host allocation 的 device 可用地址，不是额外分配的两个数组。使用完成后，应先对 `a`、`b` 调用 `cudaHostUnregister`，再用 `free` 释放原始 `malloc` allocation，且必须确保 GPU 已不再访问它们。该例同样存在上一示例的长度整除前提，另需检查 `malloc` 是否成功。

##### 2.6.3.1.3. Comparing Unified Memory and Mapped Memory（统一内存与映射内存的比较）

> Mapped memory makes CPU memory accessible from the GPU, but does not guarantee that all types of access, for example atomics, are supported on all systems. Unified memory guarantees that all access types are supported.

Mapped memory 使 GPU 能够访问 CPU 内存，但并不保证所有系统都支持所有访问类型，例如 atomic operation。Unified memory 则保证支持所有访问类型。

**解读（访问能力限定）：** “Unified memory 保证所有访问类型”表述过宽。例如，不支持 host native atomics 的系统（包括 HMM 系统）对 file-backed memory 的 atomic access 存在限制。原子操作还需满足适当的 scope 与同步规则；统一内存本身不会消除 data race。参见 [Unified Memory 的原子访问限制](https://docs.nvidia.com/cuda/cuda-programming-guide/04-special-topics/unified-memory.html)。

> Mapped memory remains in CPU memory, which means all GPU accesses must go through the connection between the CPU and GPU: PCIe or NVLink. Latency of accesses made across these links are significantly higher than access to GPU memory, and total available bandwidth is lower. As such, using mapped memory for all kernel memory accesses is unlikely to fully utilize GPU computing resources.

Mapped memory 始终保留在 CPU 内存中，这意味着 GPU 的所有访问都必须经过 CPU 与 GPU 之间的连接，例如 PCIe 或 NVLink。通过这些互连进行访问的延迟明显高于访问 GPU 内存，可用总带宽也更低。因此，让 kernel 的所有内存访问都使用 mapped memory，通常难以充分利用 GPU 的计算资源。

> Unified memory is most often migrated to the physical memory of the processor accessing it. After the first migration, repeated access to the same memory page or cache line by a kernel can utilize the full GPU memory bandwidth.

Unified memory 通常会迁移到访问它的处理器所在的物理内存中。首次迁移完成后，kernel 对同一个 memory page 或 cache line 的重复访问便可以利用 GPU 内存的全部带宽。

> Note: Mapped memory has also been referred to as zero-copy memory in previous documents.

注意：在以往的文档中，mapped memory 也被称为 zero-copy memory（零拷贝内存）。

**解读：** Zero-copy 表示程序不必先显式把整块数据复制到 GPU allocation，并不表示没有数据经过互连。Mapped memory 的访问仍会产生传输；如果 kernel 会反复访问大量数据，是否让数据驻留在 GPU 本地内存，往往比是否省下一次显式复制更重要。

> Prior to all CUDA applications using a unified virtual address space, additional APIs were needed to enable memory mapping (`cudaSetDeviceFlags` with `cudaDeviceMapHost`). These APIs are no longer needed.

在所有 CUDA 应用都使用统一虚拟地址空间之前，需要通过额外的 API 启用内存映射，即调用 `cudaSetDeviceFlags` 并指定 `cudaDeviceMapHost`。现在已不再需要这些 API。

**解读（适用范围）：** 在 CUDA Runtime 默认创建的 context 中，映射能力已隐式启用。若使用由 Driver API 创建的 context，仍应检查其配置，不能将这里的说明无条件推广到所有 context。参见 [Device Flags 的官方说明](https://docs.nvidia.com/cuda/cuda-runtime-api/group__CUDART__DEVICE.html)。

> Atomic functions (see Atomic Functions) operating on mapped host memory are not atomic from the point of view of the host or other GPUs.

对 mapped host memory 执行的 atomic function（参见 Atomic Functions）从 host 或其他 GPU 的角度来看并不具有原子性。

> CUDA runtime requires that 1-byte, 2-byte, 4-byte, 8-byte, and 16-byte naturally aligned loads and stores to host memory initiated from the device are preserved as single accesses from the point of view of the host and other devices. On some platforms, atomics to memory may be broken by the hardware into separate load and store operations. These component load and store operations have the same requirements on preservation of naturally aligned accesses. The CUDA runtime does not support a PCI Express bus topology where a PCI Express bridge splits 8-byte naturally aligned operations and NVIDIA is not aware of any topology that splits 16-byte naturally aligned operations.

CUDA Runtime 要求：device 对 host memory 发起的 1、2、4、8 和 16 字节自然对齐的 load 和 store，从 host 以及其他 device 的角度来看，必须保持为单次访问。在某些平台上，硬件可能将针对内存的 atomic operation 拆分为独立的 load 和 store。拆分后的各个 load 和 store 同样必须满足自然对齐访问不可拆分的要求。CUDA Runtime 不支持 PCI Express bridge 会拆分 8 字节自然对齐操作的 PCI Express 总线拓扑；NVIDIA 也尚未发现会拆分 16 字节自然对齐操作的拓扑。

**解读：** “自然对齐的单次 load/store 不被拆分”与“完整的 read-modify-write 原子操作”不是同一保证。如果硬件把一个 atomic operation 拆成 load 和 store，即便这两次访问各自保持完整，中间仍可能穿插其他处理器的访问。因此，不能仅凭地址对齐或 kernel 中用了 atomic function，就断言 CPU 与多个 GPU 之间的共享更新是原子的。

### 2.6.4. Summary（总结）

- > On Linux platforms with heterogeneous memory management (HMM) or address translation services (ATS), all system-allocated memory is managed memory

  在具有 HMM 或 ATS 的 Linux 平台上，所有通过系统接口分配的内存都是 managed memory。

- > On Linux platforms without HMM or ATS, on Tegra processors, and on all Windows platforms, managed memory must be allocated using CUDA:

  在不具有 HMM 或 ATS 的 Linux 平台、Tegra 处理器以及所有 Windows 平台上，必须使用 CUDA 分配 managed memory：

  - > `cudaMallocManaged` or

    使用 `cudaMallocManaged`；或者

  - > `cudaMallocFromPoolAsync` with a pool created with allocType=`cudaMemAllocationTypeManaged`

    使用 `cudaMallocFromPoolAsync`，并传入创建时设置了 `allocType=cudaMemAllocationTypeManaged` 的 memory pool。

  - > Global variables with `__managed__` specifier

    使用带有 `__managed__` 限定符的 global variable。

- > On Windows and Tegra processors, unified memory has limitations

  在 Windows 和 Tegra 处理器上，unified memory 存在限制。

**解读（小结限定）：** 本章正文使用“部分 Tegra 系统”，这里概括为“Tegra”时省略了这一限定。实践中应按前面的属性查询结果判断支持模式，避免只按产品系列名称分类。

- > On NVLINK C2C connected systems with ATS, device memory allocated with `cudaMallocManaged` can be directly accessed from the CPU or other GPUs

  在通过 NVLink C2C 连接且支持 ATS 的系统中，CPU 或其他 GPU 可以直接访问使用 `cudaMallocManaged` 分配的 device memory。

**解读：** 本节可以按三个问题理解：同一指针是否可被不同处理器使用、数据当前放在哪里、访问是否需要迁移或经过互连。UVA、managed memory 和 mapped memory 分别涉及这些问题的不同方面。选择内存方案时，先确认支持模式与访问合法性，再结合实际数据复用和同步关系评估性能。

