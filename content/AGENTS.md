# Vault 操作规则

本仓库是长期维护的个人知识库。用户的最新明确要求决定本次目标、范围和授权；角色、模板、历史报告与旧任务不能扩大授权。

## 默认工作方式

- 先判断任务规模，再读取完成任务真正需要的文件。不要仅因开始一次对话就读取整套维护文档、历史项目或仓库地图。
- 明确且可逆的工作直接完成。只有会改变结果的关键信息缺失、操作不可逆、涉及外部发布/通信，或写入范围确有冲突时才暂停询问。
- 验证强度与变更风险相称。小型、可逆修改检查目标文件和差异即可；跨目录迁移、批量改名、链接修复等才建立基线、映射、回退方案并分批验证。
- 保留已有用户改动；不执行全库恢复、自动 Git 提交，不删除有内容文件，不清空目录，不改变一级领域，除非用户针对该动作明确授权。
- 输出使用简洁、连贯的中文，保留标准英文术语。先说明结果；列表只用于并列、步骤或比较，不堆叠模板化说明。

## 按需路由

只在任务匹配时读取以下文档：

- 通用权限、写入和知识库边界：`AI-Workspace/01-Rules/00-Collaboration.md`
- 长流程、多对话、并行写入或正式交接：`01-Rules/02-Multi-AI-Protocol.md`
- 新建或实质编辑知识内容：`01-Rules/04-Knowledge-Quality.md`
- 目录迁移、批量命名或链接修复：`01-Rules/03-Procedure.md`；需要架构原则时再读 `01-Rules/01-Guiding-Prompt.md`
- 已存在的项目任务：只读该项目最新 README、相关任务单和直接依赖；不要先读全部历史报告
- 可复制任务入口：`02-Prompts/README.md`

路径均相对于根目录 `AI-Workspace`，除非已写出完整路径。

## 任务规模

- **快速任务**：一次对话、目标明确、少量文件、无并行依赖。直接执行并简要报告；不建立项目、任务单、专家登记、协调台记录、交接文件或全量哈希基线。
- **标准任务**：范围较大但可由一个对话连续完成。先确认目标、写入范围和完成标准；只为风险点保留必要记录。
- **项目任务**：跨阶段、需要多对话/多人、存在并行写入、批量高影响变更，或用户要求持续追踪。使用协作协议、项目 README 与任务单；协调台只登记真实的进行中项目。

若规模不确定，选择能安全完成工作的最轻模式。任务进行中只有在复杂度实际上升时升级，不为满足模板而升级。

## Vault 边界

一级领域保持为 `00-ToolKit`、`01-ComputerScience`、`02-AISystem`、`03-Algorithm`、`04-IntegratedCircuit`、`05-PersonalDevelopment`。`Educated` 保留为 Daily Notes 与规划复盘空间；根目录及主题附件目录需维护引用完整性。

默认不批量改写未指定的知识正文，不批量添加 Metadata、MOC 或模板，不自动拆分、合并或润色邻近笔记。明确指定的新建、编辑、移动、重命名或链接修复只对该范围生效。

Zoottelkeeper 自动生成区块不手工覆写。目录迁移期间检查 WikiLink、Markdown/HTML 链接、附件、Canvas、Base、Frontmatter 与插件配置；自动索引变化单独识别，不因已知联动反复暂停。

## 协作与写入

单对话任务无需扮演完整“部门”或登记角色。确有多个对话同时工作时，同一目标文件同一阶段只设一个写入负责人；其他对话交付独立草稿或审阅意见。写回前仅对可能重叠的目标检查实时版本，冲突只暂停重叠部分。

任务需要独立审阅时必须由另一对话或明确独立的审阅者完成；普通低影响任务可自检。共享规则及协调台只在项目模式下由指定维护负责人更新。

## 内容质量

生成或实质编辑知识笔记时必须读取知识质量准则；普通问答、计划和维护任务不因此加载。知识正文以中文解释为主，保留标准术语，区分事实、推论、个人理解和未知。引用应可核查，不虚构来源、版本、实验或个人经历。成文结构、Metadata、WikiLink、MOC、源码、实验、论文等详细规范仅在相关内容任务中按需读取；非知识笔记输出按具体任务与文体调整。
